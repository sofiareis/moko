import React, { useState } from 'react';
import {
  SafeAreaView,
  StyleSheet,
  ScrollView,
  View,
  Text,
  TouchableOpacity,
  Dimensions
} from 'react-native';

function EditCreateStoreItem({ navigation }) {
  return (
    <View>
      <Text>Hello edit create</Text>
    </View>
  );
}

export default EditCreateStoreItem;
